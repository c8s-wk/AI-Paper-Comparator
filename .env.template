# Copy this file to ".env" and fill in your secrets.

# OpenAI API key for backend LLM analysis
OPENAI_API_KEY=sk-your-key-here

# ---- Optional tuning knobs (backend/comparator) ----
# Cosine distance thresholds (smaller distance = more similar)
SIM_THRESHOLD_EXACT=0.1          # <0.1 -> verbatim
SIM_THRESHOLD_SUSPICIOUS=0.4     # <0.4 -> paraphrasing

# Text chunking for micro comparison
CHUNK_SIZE=500                   # characters per sub-chunk
CHUNK_OVERLAP=100                # overlap between chunks

# Masked robustness check (set MASK_RUNS=0 to disable)
MASK_RUNS=3
MASK_RATIO=0.5                   # 0~1, fraction of tokens/chars to mask

# LLM model & timeout
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT=30                # seconds
# OPENAI_BASE_URL=               # optional: custom endpoint/proxy
